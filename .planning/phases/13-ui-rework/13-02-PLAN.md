---
phase: 13-ui-rework
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - dashboard/index.html
  - dashboard/src/app.tsx
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/components/Settings.tsx
  - dashboard/src/components/Header.tsx
autonomous: true
requirements: [UI-01, UI-05]

must_haves:
  truths:
    - "Dashboard uses a fixed sidebar on the left with Feed, Performance, and Settings nav items"
    - "Main content area fills remaining space on the right and shows the selected view"
    - "Visual design follows a bold, committed aesthetic direction chosen by the frontend-design skill"
    - "Settings page is redesigned with improved layout"
    - "Header stats (P&L, win rate, open positions) are integrated into the new layout"
    - "DRY RUN banner still displays when dryRun mode is active"
  artifacts:
    - path: "dashboard/src/components/Sidebar.tsx"
      provides: "Fixed navigation sidebar"
      min_lines: 30
    - path: "dashboard/src/app.tsx"
      provides: "Sidebar + content grid layout"
      contains: "gridTemplateColumns"
    - path: "dashboard/index.html"
      provides: "Updated CSS variables and base styles"
      contains: "--bg"
    - path: "dashboard/src/components/Settings.tsx"
      provides: "Redesigned settings form"
      min_lines: 50
  key_links:
    - from: "dashboard/src/app.tsx"
      to: "dashboard/src/components/Sidebar.tsx"
      via: "import and render"
      pattern: "import.*Sidebar"
    - from: "dashboard/src/components/Sidebar.tsx"
      to: "dashboard/src/app.tsx"
      via: "onNavigate callback"
      pattern: "onNavigate|activeView"
---

<objective>
Replace the horizontal tab navigation with a sidebar + content layout and establish the visual design direction for the entire dashboard.

Purpose: The sidebar layout is the foundation for all other UI rework. Establishing the aesthetic direction here ensures consistency across feed cards (Plan 03) and performance charts (Plan 04). The Header stats move into the sidebar or a header bar within the new layout structure.

Output: New Sidebar component, restructured App layout, redesigned Settings, updated CSS variables/base styles, cohesive design system.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ui-rework/13-CONTEXT.md
@.planning/phases/13-ui-rework/13-RESEARCH.md
@.planning/phases/13-ui-rework/13-01-SUMMARY.md

<interfaces>
<!-- Current components to rework -->

From dashboard/src/app.tsx:
```typescript
// Current: horizontal tab nav with useState<Tab>
type Tab = 'feed' | 'performance' | 'settings';
// Rework to: sidebar + content CSS grid layout
// Pattern: <div style={{ display: 'grid', gridTemplateColumns: '220px 1fr', height: '100vh' }}>
```

From dashboard/src/components/Header.tsx:
```typescript
// Stats: openPositions, winRate, totalPnlSol fetched from /api/stats every 5s
// DRY RUN banner shown when configSignal.value?.dryRun is true
// Must be preserved in the new layout (move into sidebar or keep as top bar)
```

From dashboard/src/store/config.ts:
```typescript
export const configSignal = signal<Record<string, any>>({});
export async function fetchConfig(): Promise<void>;
export async function saveConfig(updates: Record<string, any>): Promise<{ ok: boolean; error?: string }>;
```

CSS variables from dashboard/index.html:
```css
:root {
  --bg: #0d0d0d; --bg2: #1a1a1a; --border: #2a2a2a;
  --green: #00ff88; --red: #ff4444; --blue: #4488ff;
  --yellow: #ffcc00; --gray: #666; --text: #e0e0e0;
  --mono: 'Courier New', Courier, monospace;
}
```
</interfaces>

**IMPORTANT: This plan MUST invoke the frontend-design skill for all visual decisions.**
Read and follow: `C:/Users/jackc/.claude/plugins/cache/claude-plugins-official/frontend-design/55b58ec6e564/skills/frontend-design/SKILL.md`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish design system -- update CSS variables, base styles, and create Sidebar component</name>
  <files>dashboard/index.html, dashboard/src/components/Sidebar.tsx</files>
  <action>
**Before writing any code, read and apply the frontend-design skill:**
`C:/Users/jackc/.claude/plugins/cache/claude-plugins-official/frontend-design/55b58ec6e564/skills/frontend-design/SKILL.md`

1. Choose a BOLD aesthetic direction appropriate for a Solana trading terminal. This is not a generic admin panel — it is a high-frequency trading dashboard. The design must feel like a real trading terminal. Choose a distinctive direction: industrial/utilitarian, retro-futuristic terminal, brutalist/raw, etc. Commit fully.

2. Update `dashboard/index.html`:
   - Extend CSS variables (do NOT remove existing ones — other components still reference them). ADD new variables for the design system: additional colors, spacing scale, border radius, shadow values, font families.
   - Add a Google Fonts link for a distinctive display/heading font (NOT Inter, Roboto, Arial, or system fonts — pick something with character). Keep monospace for data values.
   - Update body/base styles to match the new direction.

3. Create `dashboard/src/components/Sidebar.tsx`:
   - Fixed sidebar, ~220px wide, full viewport height.
   - Navigation items: Feed, Performance, Settings (per locked decision).
   - Active state clearly indicated (not just color — think about shape, background, border, indicator bar).
   - Brand/title area at top ("SOLSNIPER" or similar).
   - Stats summary: integrate the Header.tsx stats (P&L, win rate, open positions) into the sidebar bottom area. Fetch from /api/stats every 5s (same pattern as current Header.tsx).
   - SSE connection status indicator (derive from a simple state: show a dot/indicator based on whether events are flowing).
   - DRY RUN indicator if configSignal.value?.dryRun is true.
   - Props: `activeView: 'feed' | 'performance' | 'settings'` and `onNavigate: (view: View) => void`.

Design the sidebar with the full aesthetic commitment the frontend-design skill demands. This sidebar sets the tone for the entire application.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && rtk vitest run 2>/dev/null; echo "---"; test -f dashboard/src/components/Sidebar.tsx && echo "Sidebar.tsx exists" || echo "MISSING"</automated>
  </verify>
  <done>CSS variables extended with new design system values. Google Font loaded. Sidebar.tsx created with nav items, stats, connection indicator, DRY RUN badge, and a bold aesthetic direction.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure App layout to sidebar + content grid and redesign Settings</name>
  <files>dashboard/src/app.tsx, dashboard/src/components/Settings.tsx, dashboard/src/components/Header.tsx</files>
  <action>
**Continue applying the frontend-design skill aesthetic established in Task 1.**

1. Rewrite `dashboard/src/app.tsx`:
   - Replace horizontal tab nav with CSS grid: `gridTemplateColumns: '220px 1fr'` (or the sidebar width chosen in Task 1).
   - Import and render Sidebar on the left.
   - Main content area on the right renders the active view (Feed, Performance, Settings).
   - Remove the old `<nav>` tab bar and TAB_STYLE.
   - Keep the view state: `const [view, setView] = useState<View>('feed')`.
   - Type alias: `type View = 'feed' | 'performance' | 'settings'`.

2. Update `dashboard/src/components/Header.tsx`:
   - The Header component may be removed entirely if its stats are now in the Sidebar. OR, keep a minimal top bar within the content area showing the current view title and/or DRY RUN banner.
   - Decision: If Sidebar already shows stats and DRY RUN indicator, remove Header and instead show DRY RUN as a full-width banner INSIDE the main content area (above the active view) — this preserves the prominent DRY RUN visibility per Phase 12 decision.
   - Keep the DRY RUN banner visible and prominent regardless of approach.

3. Redesign `dashboard/src/components/Settings.tsx`:
   - Apply the new design system: use the CSS variables and font choices from Task 1.
   - Group settings into clearly delineated sections with the new visual treatment (cards, panels, or sections — match the aesthetic).
   - Keep ALL existing functionality: dryRun toggle, minSafetyScore, buyAmountSol, maxSlippageBps, maxConcurrentPositions, stopLossPct, trailingStopPct.
   - Keep the save/status logic unchanged.
   - Improve input styling, spacing, and visual hierarchy to match the new design direction.
   - The settings layout was previously maxWidth: 600px — consider filling more of the content area width with a well-structured layout.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && pnpm build:dashboard 2>&1 | tail -5</automated>
  </verify>
  <done>App uses sidebar + content grid layout. Old tab nav removed. Settings redesigned with new visual treatment. DRY RUN banner visible. Dashboard builds without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Polish and consistency pass -- ensure all layout transitions, responsive behavior, and visual coherence</name>
  <files>dashboard/src/app.tsx, dashboard/src/components/Sidebar.tsx, dashboard/src/components/Settings.tsx, dashboard/index.html</files>
  <action>
**Final pass applying frontend-design skill refinement.**

1. Ensure smooth visual transitions between views (feed/performance/settings). When clicking a sidebar nav item, content area switches cleanly without layout shifts.

2. Add CSS transitions or micro-animations per the frontend-design skill: hover states on sidebar items, subtle load reveals, focus states on Settings inputs. CSS-only — no animation libraries.

3. Verify the DRY RUN banner is prominent in both scenarios:
   - If sidebar has a DRY RUN indicator (compact), also show a content-area banner.
   - The banner must use `configSignal.value?.dryRun` (Preact signal auto-subscription, no useEffect).

4. Ensure the layout works reasonably at common widths (1280px+). Full responsive/mobile is Claude's Discretion — at minimum, the sidebar should not overlap content at 1024px width. Consider collapsing the sidebar to icons at narrow widths if appropriate.

5. Remove any dead code from the old Header.tsx if it was replaced. If Header.tsx is now empty or unnecessary, delete the file and remove its import from app.tsx.

6. Verify all CSS variables from index.html are still valid — existing components (LiveFeed, Performance) still reference the original variables until they are reworked in Plans 03 and 04. Do NOT break them.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && pnpm build:dashboard 2>&1 | tail -5</automated>
  </verify>
  <done>Layout is polished: hover states, transitions, DRY RUN banner prominent, responsive at 1024px+, no dead code, all CSS variables intact, dashboard builds cleanly.</done>
</task>

</tasks>

<verification>
- Dashboard renders with sidebar on left, content on right
- Sidebar shows Feed, Performance, Settings nav items with active state
- Sidebar shows P&L, win rate, open positions stats
- DRY RUN banner displays when dryRun is active
- Settings page has improved layout with all fields functional
- Old horizontal tab nav is gone
- `pnpm build:dashboard` succeeds
- No existing CSS variables broken
</verification>

<success_criteria>
Dashboard layout transformed from horizontal tabs to sidebar + content. Bold aesthetic direction established and applied consistently. Settings redesigned. Stats integrated into sidebar. DRY RUN visibility preserved. Dashboard builds and all views render.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-rework/13-02-SUMMARY.md`
</output>
