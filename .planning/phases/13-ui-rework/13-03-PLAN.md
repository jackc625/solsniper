---
phase: 13-ui-rework
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - dashboard/src/components/LiveFeed.tsx
  - dashboard/src/components/FeedCard.tsx
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "Feed shows rich cards instead of flat text rows"
    - "Each card displays: event type badge, full mint (clickable Solscan link), safety score, source, buy amount, timestamp"
    - "PumpPortal-sourced tokens also show a pump.fun link"
    - "Cards are expandable for additional detail"
    - "Auto-scroll to bottom works on new events (not on card expansion)"
    - "DRY RUN badge appears on dry-run trade cards"
    - "All links open in new tab"
  artifacts:
    - path: "dashboard/src/components/FeedCard.tsx"
      provides: "Rich expandable card component"
      min_lines: 40
    - path: "dashboard/src/components/LiveFeed.tsx"
      provides: "Reworked feed using FeedCard"
      contains: "FeedCard"
  key_links:
    - from: "dashboard/src/components/LiveFeed.tsx"
      to: "dashboard/src/components/FeedCard.tsx"
      via: "import and map render"
      pattern: "import.*FeedCard"
    - from: "dashboard/src/components/FeedCard.tsx"
      to: "solscan.io"
      via: "anchor tag href"
      pattern: "solscan\\.io/token"
    - from: "dashboard/src/components/FeedCard.tsx"
      to: "pump.fun"
      via: "conditional anchor tag"
      pattern: "pump\\.fun/coin"
    - from: "dashboard/src/components/LiveFeed.tsx"
      to: "dashboard/src/store/feed.ts"
      via: "feedEvents signal"
      pattern: "feedEvents\\.value"
---

<objective>
Replace the flat text log rows in LiveFeed with rich, expandable trading cards that show all relevant trade data at a glance.

Purpose: The feed is the primary view of the dashboard. Cards must feel like a real trading terminal feed, not a text log. Each card should give the operator immediate context: what token, from where, how much, how safe, what happened.

Output: New FeedCard component with expand/collapse, reworked LiveFeed that renders FeedCards.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ui-rework/13-CONTEXT.md
@.planning/phases/13-ui-rework/13-RESEARCH.md
@.planning/phases/13-ui-rework/13-01-SUMMARY.md

<interfaces>
<!-- Enriched FeedEvent from Plan 01 -->

From dashboard/src/store/feed.ts (after Plan 01):
```typescript
export interface FeedEvent {
  type: string;
  mint: string;
  ts: number;
  detail?: string;
  isDryRun?: boolean;
  safetyScore?: number;
  source?: string;
  buyAmountSol?: number;
}
```

BADGE_COLORS mapping (preserve these event type to color mappings):
```typescript
TOKEN_DETECTED: gray, BUY_SENT: blue, BUY_CONFIRMED: green,
BUY_FAILED: red, SELL_TRIGGERED: yellow, SELL_CONFIRMED: green,
SELL_FAILED: red, ERROR: red
```

External link patterns:
```typescript
const SOLSCAN_TOKEN = (mint: string) => `https://solscan.io/token/${mint}`;
const PUMPFUN_TOKEN = (mint: string) => `https://pump.fun/coin/${mint}`;
// All links: target="_blank" rel="noopener noreferrer"
```

Auto-scroll pattern from current LiveFeed.tsx:
```typescript
// isLive state tracks whether user has scrolled up
// Auto-scroll fires on feedEvents.value change (not on card expansion)
// "Resume Live" button when user scrolls up
```

CSS variables (from index.html, may be extended by Plan 02):
```css
--bg, --bg2, --border, --green, --red, --blue, --yellow, --gray, --text, --mono
```
</interfaces>

**IMPORTANT: This plan MUST invoke the frontend-design skill for all visual decisions.**
Read and follow: `C:/Users/jackc/.claude/plugins/cache/claude-plugins-official/frontend-design/55b58ec6e564/skills/frontend-design/SKILL.md`

**NOTE:** This plan runs in parallel with Plan 02 (different files). Use the CSS variables from `dashboard/index.html` for styling. If Plan 02 has already run and added new variables, use those too. If not, the existing variables are sufficient.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeedCard component with rich layout and expandable detail</name>
  <files>dashboard/src/components/FeedCard.tsx</files>
  <action>
**Read and apply the frontend-design skill:**
`C:/Users/jackc/.claude/plugins/cache/claude-plugins-official/frontend-design/55b58ec6e564/skills/frontend-design/SKILL.md`

Create `dashboard/src/components/FeedCard.tsx`:

1. **Collapsed view** (always visible) — a card/panel with:
   - **Event type badge**: colored badge matching the event type (use BADGE_COLORS mapping). Badge should be visually prominent (filled background, not just border).
   - **Token mint**: FULL mint address (not shortened — per locked decision), displayed as a clickable link to `https://solscan.io/token/{mint}`. Use `target="_blank" rel="noopener noreferrer"`. Style the mint in monospace. Consider truncating with ellipsis CSS (`text-overflow: ellipsis, overflow: hidden`) if it overflows, but show full mint on hover via `title` attribute.
   - **Source badge**: if `event.source` is available, show "PumpPortal" or "Raydium" or "PumpSwap" as a small label/tag.
   - **pump.fun link**: if `event.source === 'pumpportal'`, show an additional link to `https://pump.fun/coin/{mint}` (small icon or text link).
   - **Safety score**: if `event.safetyScore` is available, show it (e.g., "Score: 85" or a visual indicator like a colored bar). Color-code: green for high (70+), yellow for moderate (40-69), red for low (<40).
   - **Buy amount**: if `event.buyAmountSol` is available, show it (e.g., "0.01 SOL").
   - **Timestamp**: formatted as HH:MM:SS.
   - **DRY RUN badge**: if `event.isDryRun`, show a prominent badge (per Phase 12 pattern — yellow border + text). Reduce card opacity slightly for dry-run events (0.7-0.8).

2. **Expanded view** (on click/toggle):
   - Use `useState(false)` for local expand state (NOT signals — per-card state is local).
   - Show `event.detail` (sell ladder status, error messages, etc.).
   - Show additional context: full mint (if truncated in collapsed), any other fields from FeedEvent.
   - Subtle expand/collapse animation (CSS max-height transition or similar).
   - Click anywhere on the card header to toggle (not just a button).

3. **Visual design**:
   - Cards should have clear visual boundaries (background, border, subtle shadow, or similar).
   - Slight spacing between cards.
   - Hover state: subtle highlight or border change.
   - Different event types should feel visually distinct (not just the badge color — consider left border accent, background tint, etc.).
   - Design must feel like a trading terminal feed, not a generic list.

Props: `{ event: FeedEvent }` — import FeedEvent from store/feed.ts.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && test -f dashboard/src/components/FeedCard.tsx && echo "FeedCard.tsx exists" && pnpm build:dashboard 2>&1 | tail -3</automated>
  </verify>
  <done>FeedCard.tsx exists with collapsed/expanded views, Solscan link, pump.fun link for PumpPortal tokens, safety score indicator, buy amount, DRY RUN badge, and bold visual design.</done>
</task>

<task type="auto">
  <name>Task 2: Rework LiveFeed to render FeedCards with preserved auto-scroll behavior</name>
  <files>dashboard/src/components/LiveFeed.tsx</files>
  <action>
**Continue applying the frontend-design skill aesthetic from Task 1.**

Rewrite `dashboard/src/components/LiveFeed.tsx`:

1. Replace `FeedRow` with `FeedCard`:
   - Remove the old `FeedRow` internal component and `shortenMint` helper.
   - Import `FeedCard` from `./FeedCard.js`.
   - Map over `feedEvents.value` rendering `<FeedCard key={...} event={e} />`.

2. **Preserve auto-scroll behavior exactly**:
   - Keep `listRef`, `isLive` state, `handleScroll` logic.
   - Auto-scroll triggers on `feedEvents.value` change ONLY (NOT on card expansion — per pitfall #4 from RESEARCH.md).
   - "Resume Live" button appears when user scrolls up.
   - "LIVE" indicator when at bottom.

3. **Update the toolbar area** (the bar with "Resume Live" / "LIVE" indicator):
   - Style it to match the new design system.
   - Consider showing event count badge: `feedEvents.value.length` events.
   - Keep it functional and unobtrusive.

4. **Empty state**: when no events, show a styled empty state (not just plain gray text). Something appropriate to the design direction — a pulse animation, terminal cursor blink, etc.

5. **Keep BADGE_COLORS** mapping or move it to FeedCard.tsx (wherever it is used). The color mapping by event type must be preserved.

6. Remove dead code: the old `FeedRow` component, old inline styles.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && pnpm build:dashboard 2>&1 | tail -5</automated>
  </verify>
  <done>LiveFeed renders FeedCards. Auto-scroll works on new events. "Resume Live" button works. Empty state styled. Dashboard builds cleanly.</done>
</task>

</tasks>

<verification>
- Feed shows rich cards (not flat text rows)
- Each card shows event badge, mint (clickable Solscan link), source, safety score, buy amount, timestamp
- PumpPortal tokens show pump.fun link
- Cards expand on click to show detail
- Auto-scroll works on new events, does NOT trigger on card expansion
- DRY RUN badge visible on dry-run cards
- All links open in new tab
- `pnpm build:dashboard` succeeds
</verification>

<success_criteria>
LiveFeed transformed from text log to rich trading card feed. Cards show all enriched data (safety score, source, buy amount) with clickable external links. Expand/collapse works. Auto-scroll preserved. Visual design is bold and distinctive per frontend-design skill.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-rework/13-03-SUMMARY.md`
</output>
