---
phase: 14-sell-price-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/utils/parse-sol-received.ts
  - src/execution/sell/standard-seller.ts
  - src/execution/sell/jito-seller.ts
  - src/execution/sell/pump-portal-seller.ts
  - src/execution/sell/chunked-seller.ts
  - src/execution/sell/standard-seller.test.ts
  - src/execution/sell/chunked-seller.test.ts
  - src/execution/sell/pump-portal-seller.test.ts
  - src/execution/sell/jito-seller.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "standardSell returns an object with signature and solReceived (not just a string)"
    - "jitoSell returns an object with signature and solReceived from Jupiter quote outAmount"
    - "pumpPortalSell returns an object with signature and solReceived parsed from on-chain tx"
    - "chunkedSell returns an object with confirmedTranches and accumulated solReceived"
    - "parseSolReceived is a shared utility reusable by both pump-portal-seller and sell-ladder"
  artifacts:
    - path: "src/types/index.ts"
      provides: "SellOutcome and ChunkedSellOutcome interfaces"
      contains: "SellOutcome"
    - path: "src/utils/parse-sol-received.ts"
      provides: "Shared on-chain SOL balance delta parser"
      exports: ["parseSolReceived"]
    - path: "src/execution/sell/standard-seller.ts"
      provides: "standardSell returning SellOutcome with solReceived from quoteResponse.outAmount / 1e9"
      contains: "solReceived"
    - path: "src/execution/sell/jito-seller.ts"
      provides: "jitoSell returning SellOutcome with solReceived from quoteResponse.outAmount / 1e9"
      contains: "solReceived"
    - path: "src/execution/sell/pump-portal-seller.ts"
      provides: "pumpPortalSell returning SellOutcome with solReceived from on-chain pre/post balance delta"
      contains: "parseSolReceived"
    - path: "src/execution/sell/chunked-seller.ts"
      provides: "chunkedSell returning ChunkedSellOutcome with accumulated solReceived across tranches"
      contains: "ChunkedSellOutcome"
    - path: "src/execution/sell/standard-seller.test.ts"
      provides: "Tests for standardSell SellOutcome return type"
      contains: "solReceived"
    - path: "src/execution/sell/chunked-seller.test.ts"
      provides: "Tests for chunkedSell ChunkedSellOutcome return type"
      contains: "solReceived"
  key_links:
    - from: "src/execution/sell/standard-seller.ts"
      to: "jupiterClient.quote"
      via: "quoteResponse.outAmount / 1e9 extraction"
      pattern: "Number.*outAmount.*1e9"
    - from: "src/execution/sell/pump-portal-seller.ts"
      to: "src/utils/parse-sol-received.ts"
      via: "imports parseSolReceived for on-chain SOL balance delta"
      pattern: "parseSolReceived"
    - from: "src/execution/sell/chunked-seller.ts"
      to: "src/execution/sell/standard-seller.ts"
      via: "destructures SellOutcome from standardSell calls"
      pattern: "solReceived"
---

<objective>
Change all four seller functions from returning `Promise<string>` (signature only) to returning structured objects that include the SOL received amount. Extract on-chain transaction parse to a shared utility so it can be reused by both pump-portal-seller and sell-ladder (for EMERGENCY step). Create Wave 0 test files for standard-seller and chunked-seller.

Purpose: Without this change, `sellPriceSol` is never computed and `sell_price_sol` stays NULL in SQLite forever, making all P&L tracking impossible.
Output: All sellers return `SellOutcome` or `ChunkedSellOutcome` with `solReceived` populated. Shared `parseSolReceived` utility exists for on-chain parse reuse.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-sell-price-bug-fixes/14-CONTEXT.md
@.planning/phases/14-sell-price-bug-fixes/14-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/types/index.ts:
```typescript
export interface BroadcastResult {
  signature: string;
  blockhash: string;
  lastValidBlockHeight: number;
}

export interface SellResult {
  success: boolean;
  step?: SellStep;
  signature?: string;
  errorMessage?: string;
}
```

From src/execution/jupiter-client.ts (quote return shape):
```typescript
// jupiterClient.quote() returns an object with at minimum:
// { outAmount: string } -- lamports of expected SOL output
```

From src/execution/broadcaster.ts:
```typescript
// broadcastAndConfirm() returns BroadcastResult { signature, blockhash, lastValidBlockHeight }
```

From src/position/position-manager.ts (reference pattern at line 422-423):
```typescript
const data = (await this.jupiterClient.quote(params)) as { outAmount: string };
return Number(data.outAmount) / 1e9;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test scaffolds for standard-seller and chunked-seller</name>
  <files>src/execution/sell/standard-seller.test.ts, src/execution/sell/chunked-seller.test.ts</files>
  <action>
**1. Create `src/execution/sell/standard-seller.test.ts`:**

Set up a test file with mocked dependencies (jupiterClient, broadcastAndConfirm). The test must verify the new SellOutcome return type once Task 1 implements it.

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Hoisted mocks
const { mockQuote, mockSwap, mockBroadcast } = vi.hoisted(() => ({
  mockQuote: vi.fn(),
  mockSwap: vi.fn(),
  mockBroadcast: vi.fn(),
}));

vi.mock('../jupiter-client.js', () => ({
  jupiterClient: { quote: mockQuote, swap: mockSwap },
}));

vi.mock('../broadcaster.js', () => ({
  broadcastAndConfirm: mockBroadcast,
}));

import { standardSell } from './standard-seller.js';

describe('standardSell', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  it('returns SellOutcome with signature and solReceived from quoteResponse.outAmount', async () => {
    mockQuote.mockResolvedValue({ outAmount: '500000000' }); // 0.5 SOL in lamports
    mockSwap.mockResolvedValue({ swapTransaction: Buffer.from(new Uint8Array(200)).toString('base64') });
    // broadcastAndConfirm needs a real-ish VersionedTransaction; mock it to return result
    mockBroadcast.mockResolvedValue({ signature: 'test-sig-1', blockhash: 'bh', lastValidBlockHeight: 100 });

    const result = await standardSell(
      'TestMint111111111111111111111111111111111111',
      1000000n,
      { slippageBps: 500, feeMultiplier: 1 },
      { execution: { buy: { priorityFeeBaseLamports: 100000 } } } as any,
      {} as any,
      [{}] as any
    );

    expect(result).toEqual({ signature: 'test-sig-1', solReceived: 0.5 });
  });
});
```

NOTE: The VersionedTransaction.deserialize call will need the mock to supply valid bytes. If the mock throws on deserialize, adjust by mocking the VersionedTransaction import or by having mockSwap return a base64 that produces a deserializable tx. The executor should handle this pragmatically -- the key assertion is that the return type is `{ signature, solReceived }`.

**2. Create `src/execution/sell/chunked-seller.test.ts`:**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

const { mockStandardSell, mockGetAssociatedTokenAddress, mockGetAccount } = vi.hoisted(() => ({
  mockStandardSell: vi.fn(),
  mockGetAssociatedTokenAddress: vi.fn(),
  mockGetAccount: vi.fn(),
}));

vi.mock('./standard-seller.js', () => ({
  standardSell: mockStandardSell,
}));

vi.mock('@solana/spl-token', () => ({
  getAssociatedTokenAddress: mockGetAssociatedTokenAddress,
  getAccount: mockGetAccount,
  TOKEN_PROGRAM_ID: 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TOKEN_2022_PROGRAM_ID: 'TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb',
}));

import { chunkedSell } from './chunked-seller.js';

describe('chunkedSell', () => {
  beforeEach(() => { vi.clearAllMocks(); });

  it('returns ChunkedSellOutcome with accumulated solReceived across tranches', async () => {
    mockGetAssociatedTokenAddress.mockResolvedValue('ata-address');
    mockGetAccount.mockResolvedValue({ amount: 3000000n });
    mockStandardSell
      .mockResolvedValueOnce({ signature: 'sig-t1', solReceived: 0.3 })
      .mockResolvedValueOnce({ signature: 'sig-t2', solReceived: 0.3 })
      .mockResolvedValueOnce({ signature: 'sig-t3', solReceived: 0.4 });

    const result = await chunkedSell(
      'TestMint111111111111111111111111111111111111',
      { execution: { sell: { standardSlippageBps: 500, highFeeMultiplier: 3 } } } as any,
      {} as any,
      [{}] as any
    );

    expect(result).toEqual({ confirmedTranches: 3, solReceived: 1.0 });
  });

  it('returns zero confirmedTranches with undefined solReceived on zero balance', async () => {
    mockGetAssociatedTokenAddress.mockResolvedValue('ata-address');
    mockGetAccount.mockResolvedValue({ amount: 0n });

    const result = await chunkedSell(
      'TestMint111111111111111111111111111111111111',
      { execution: { sell: { standardSlippageBps: 500, highFeeMultiplier: 3 } } } as any,
      {} as any,
      [{}] as any
    );

    expect(result).toEqual({ confirmedTranches: 0, solReceived: undefined });
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run src/execution/sell/standard-seller.test.ts src/execution/sell/chunked-seller.test.ts 2>&1 | head -30</automated>
  </verify>
  <done>Wave 0 test scaffolds exist for standard-seller and chunked-seller. Tests will initially fail (RED) until Task 1 implements the new return types.</done>
</task>

<task type="auto">
  <name>Task 1: Define SellOutcome/ChunkedSellOutcome types, extract parseSolReceived utility, and update all 4 sellers</name>
  <files>src/types/index.ts, src/utils/parse-sol-received.ts, src/execution/sell/standard-seller.ts, src/execution/sell/jito-seller.ts, src/execution/sell/pump-portal-seller.ts, src/execution/sell/chunked-seller.ts, src/execution/sell/pump-portal-seller.test.ts, src/execution/sell/jito-seller.test.ts</files>
  <action>
**1. Add new types to `src/types/index.ts`:**

Add after the existing `SellResult` interface:

```typescript
/** Return type for Jupiter-based sellers (standard, high-fee, jito, emergency, pumpportal). */
export interface SellOutcome {
  signature: string;
  solReceived?: number;  // SOL received from sell (undefined only on true parse failure)
}

/** Return type for chunked seller (multiple tranches). */
export interface ChunkedSellOutcome {
  confirmedTranches: number;
  solReceived?: number;  // sum of outAmounts across confirmed tranches
}
```

**2. Create `src/utils/parse-sol-received.ts` (shared utility):**

Extract the on-chain SOL balance delta parser to a shared module so both `pump-portal-seller.ts` and `sell-ladder.ts` (for EMERGENCY step) can reuse it:

```typescript
/**
 * parse-sol-received.ts -- Parses actual SOL received from a confirmed on-chain transaction.
 *
 * Used by:
 * - pump-portal-seller.ts: PumpPortal has no Jupiter quote, must parse on-chain
 * - sell-ladder.ts: EMERGENCY step (49% slippage) where Jupiter quote is unreliable
 *
 * Method: Compares wallet's pre/post SOL balance delta from the transaction metadata.
 */
import type { Connection, PublicKey } from '@solana/web3.js';
import { createModuleLogger } from '../core/logger.js';

const log = createModuleLogger('parse-sol-received');

/**
 * Parses the actual SOL received by the wallet from a confirmed transaction.
 * Returns the SOL balance increase (post - pre) or undefined if parse fails.
 *
 * @param signature     - Confirmed transaction signature
 * @param walletPubKey  - Wallet public key to find in account keys
 * @param connection    - RPC connection for getTransaction
 * @returns SOL received (positive delta) or undefined on any failure
 */
export async function parseSolReceived(
  signature: string,
  walletPubKey: PublicKey,
  connection: Connection
): Promise<number | undefined> {
  try {
    const tx = await connection.getTransaction(signature, {
      commitment: 'confirmed',
      maxSupportedTransactionVersion: 0,
    });
    if (!tx || !tx.meta) {
      log.warn({ signature }, 'parseSolReceived: no transaction or meta found');
      return undefined;
    }

    const accountKeys = tx.transaction.message.getAccountKeys().staticAccountKeys;
    const walletIndex = accountKeys.findIndex(k => k.equals(walletPubKey));
    if (walletIndex === -1) {
      log.warn({ signature }, 'parseSolReceived: wallet not found in account keys');
      return undefined;
    }

    const pre = tx.meta.preBalances[walletIndex];
    const post = tx.meta.postBalances[walletIndex];
    if (pre == null || post == null) return undefined;

    const delta = (post - pre) / 1e9;
    return delta > 0 ? delta : undefined;
  } catch (err) {
    log.warn({ signature, err }, 'parseSolReceived: failed to parse transaction');
    return undefined;
  }
}
```

**3. Update `src/execution/sell/standard-seller.ts`:**

- Import `SellOutcome` from `../../types/index.js`
- Change return type from `Promise<string>` to `Promise<SellOutcome>`
- After `const quoteResponse = await jupiterClient.quote(params);`, extract:
  `const solReceived = Number((quoteResponse as { outAmount: string }).outAmount) / 1e9;`
  (same pattern as `PositionManager.getPositionValueSol()`)
- Change final return from `return result.signature;` to `return { signature: result.signature, solReceived };`

**4. Update `src/execution/sell/jito-seller.ts`:**

- Import `SellOutcome` from `../../types/index.js`
- Change return type from `Promise<string>` to `Promise<SellOutcome>`
- After `const quoteResponse = await jupiterClient.quote(params);` (line 76), extract:
  `const solReceived = Number((quoteResponse as { outAmount: string }).outAmount) / 1e9;`
- DRY RUN path (line 61): return `{ signature, solReceived: undefined }` -- no quote is made in dry-run
- Normal path final return: change `return swapSignature;` to `return { signature: swapSignature, solReceived };`

**5. Update `src/execution/sell/pump-portal-seller.ts`:**

- Import `SellOutcome` from `../../types/index.js`
- Import `PublicKey` from `@solana/web3.js` (it already imports `VersionedTransaction`)
- Import `parseSolReceived` from `../../utils/parse-sol-received.js`
- Change return type from `Promise<string>` to `Promise<SellOutcome>`
- Remove the local `parseSolReceived` function (now imported from shared utility)
- After `broadcastAndConfirm`, call:
  `const solReceived = await parseSolReceived(result.signature, wallet.publicKey, connections[0]);`
- Return `{ signature: result.signature, solReceived }` instead of `result.signature`

**6. Update `src/execution/sell/chunked-seller.ts`:**

- Import `ChunkedSellOutcome` from `../../types/index.js`
- Change return type from `Promise<number>` to `Promise<ChunkedSellOutcome>`
- Add `let totalSolReceived = 0;` alongside `let confirmedTranches = 0;`
- In the tranche loop, destructure the `standardSell` result:
  ```typescript
  const outcome = await standardSell(mint, amount, { ... }, config, wallet, connections);
  confirmedTranches++;
  if (outcome.solReceived != null) totalSolReceived += outcome.solReceived;
  ```
- Change both return statements:
  - Zero balance return: `return { confirmedTranches: 0, solReceived: undefined };`
  - End of function: `return { confirmedTranches, solReceived: totalSolReceived > 0 ? totalSolReceived : undefined };`

**Important:** The `standardSell` call in the tranche loop previously returned a string that was ignored (only confirmedTranches++ mattered). Now it returns `SellOutcome` -- the destructured `outcome` object replaces the discarded await.

**7. Update existing seller tests (NOT sell-ladder.test.ts -- that's Plan 02):**

- In `src/execution/sell/pump-portal-seller.test.ts`: The test asserts `expect(result).toBe('pump-sell-sig')` -- update to `expect(result).toEqual({ signature: 'pump-sell-sig', solReceived: undefined })` (since the on-chain parse mock won't return a tx, solReceived will be undefined). Mock `connections[0].getTransaction` to return null for existing tests.
- In `src/execution/sell/jito-seller.test.ts`: Update dry-run test assertion from `expect(result).toMatch(/^DRY_RUN_JITO_/)` to check `result.signature` matches. Update dry-run=false test similarly.

**DO NOT update `sell-ladder.test.ts` mocks in this plan.** Sell-ladder mock updates are handled in Plan 02 alongside the SellLadder threading changes.
  </action>
  <verify>
    <automated>npx vitest run src/execution/sell/standard-seller.test.ts src/execution/sell/chunked-seller.test.ts src/execution/sell/pump-portal-seller.test.ts src/execution/sell/jito-seller.test.ts 2>&1 | head -50</automated>
  </verify>
  <done>All four sellers return SellOutcome/ChunkedSellOutcome with solReceived populated from Jupiter quote outAmount (STANDARD, HIGH_FEE, JITO) or on-chain transaction parse (PUMPPORTAL). Chunked seller accumulates solReceived across tranches. parseSolReceived extracted to shared utility. All seller-level tests (excluding sell-ladder.test.ts) pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/execution/sell/standard-seller.test.ts src/execution/sell/chunked-seller.test.ts src/execution/sell/pump-portal-seller.test.ts src/execution/sell/jito-seller.test.ts` passes all seller tests with new return types
- `npx tsc --noEmit` confirms type correctness
- Note: sell-ladder.test.ts will fail after this plan because its mocks still return old types. Plan 02 fixes those mocks.
</verification>

<success_criteria>
- standardSell, jitoSell return `SellOutcome { signature, solReceived }` with `solReceived = Number(quoteResponse.outAmount) / 1e9`
- pumpPortalSell returns `SellOutcome { signature, solReceived }` with `solReceived` from shared `parseSolReceived` utility
- chunkedSell returns `ChunkedSellOutcome { confirmedTranches, solReceived }` with accumulated tranche totals
- `parseSolReceived` exists as shared utility in `src/utils/parse-sol-received.ts`
- Wave 0 test files exist: standard-seller.test.ts, chunked-seller.test.ts
- All seller-level tests pass (sell-ladder.test.ts excluded -- updated in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/14-sell-price-bug-fixes/14-01-SUMMARY.md`
</output>
