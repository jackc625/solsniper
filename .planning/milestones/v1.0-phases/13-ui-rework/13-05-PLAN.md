---
phase: 13-ui-rework
plan: 05
type: execute
wave: 3
depends_on: ["13-02", "13-03", "13-04"]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "Sidebar layout renders correctly with nav, stats, and DRY RUN indicator"
    - "Feed shows rich expandable cards with all enriched data"
    - "Performance shows charts and sortable trade history table"
    - "Settings form is functional with improved design"
    - "All external links open in new tab"
    - "Visual design is cohesive across all views"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete UI rework across all views.

Purpose: This is a visual redesign phase. Automated tests cannot verify aesthetic quality, layout correctness, or interaction feel. The operator must see the dashboard and confirm it meets expectations.

Output: Human confirmation that the UI rework is acceptable.
</objective>

<execution_context>
@C:/Users/jackc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jackc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-ui-rework/13-CONTEXT.md
@.planning/phases/13-ui-rework/13-02-SUMMARY.md
@.planning/phases/13-ui-rework/13-03-SUMMARY.md
@.planning/phases/13-ui-rework/13-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete dashboard UI rework</name>
  <action>
Build the dashboard and present it for human review. Run `pnpm build:dashboard` to ensure production build succeeds. Then instruct the user to launch the dev server for visual verification.

What was built:
- Sidebar + content layout (replacing horizontal tabs)
- Rich expandable feed cards with Solscan/pump.fun links, safety scores, source badges
- Performance analytics: P&L chart, win rate visualization, sortable completed trade history table
- Redesigned Settings page
- Bold visual design via frontend-design skill

How to verify:
1. Start the bot and dashboard dev server:
   `pnpm dev` (or however the bot starts)
   In a separate terminal: `cd dashboard && pnpm exec vite`
   Open http://localhost:5173 in a browser.

2. Sidebar layout:
   - Confirm sidebar on left with Feed, Performance, Settings nav items
   - Confirm clicking each nav item switches the content area
   - Confirm stats (P&L, win rate, open positions) visible in sidebar
   - If DRY RUN mode is on, confirm DRY RUN indicator appears

3. Feed view:
   - If events are flowing, confirm rich cards (not flat text rows)
   - Confirm each card shows: event type badge, mint (clickable link), timestamp
   - Click a mint link -- should open Solscan in a new tab
   - If a PumpPortal-sourced event exists, confirm pump.fun link is shown
   - Click a card to expand -- should show additional detail
   - Scroll up to see "Resume Live" button, click it to return to bottom

4. Performance view:
   - If completed trades exist, confirm P&L chart renders (line chart)
   - Confirm trade history table shows completed trades with columns
   - Click a column header to sort
   - Click a mint in the table -- should open Solscan in new tab
   - Confirm win rate is displayed prominently
   - If no completed trades, confirm empty state is styled (not blank)

5. Settings view:
   - Confirm all settings fields present (dry run, safety score, buy amount, slippage, positions, stop-loss, trailing stop)
   - Change a value and click Save -- confirm it saves successfully

6. Overall design:
   - Confirm the design looks intentional and cohesive (not generic)
   - Confirm dark theme is maintained
   - Confirm no broken layouts, overlapping elements, or missing styles
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && pnpm build:dashboard 2>&1 | tail -3</automated>
  </verify>
  <done>User types "approved" if the UI looks good, or describes specific issues to address.</done>
</task>

</tasks>

<verification>
Human confirms all views render correctly with the new design.
</verification>

<success_criteria>
User approves the complete UI rework. All views (feed, performance, settings) look and function correctly with the new sidebar layout and visual design.
</success_criteria>

<output>
After completion, create `.planning/phases/13-ui-rework/13-05-SUMMARY.md`
</output>
