---
phase: 16-sell-partial-traceability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "DRY-01 through DRY-08 each have a definition bullet in REQUIREMENTS.md v1 section"
    - "UI-01 through UI-06 each have a definition bullet in REQUIREMENTS.md v1 section"
    - "All 14 new definitions are marked [x] (complete)"
    - "Coverage summary updated to reflect no pending requirements"
    - "SELL_PARTIAL SSE subscription exists in feed.ts eventTypes array"
    - "SELL_PARTIAL has badge color and event label in FeedCard.tsx"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DRY and UI requirement definitions"
      contains: "DRY-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "requirement IDs match phase assignments"
      pattern: "DRY-0[1-8]|UI-0[1-6]"
---

<objective>
Backfill DRY-01 through DRY-08 and UI-01 through UI-06 requirement definitions into REQUIREMENTS.md, and verify that SELL_PARTIAL frontend wiring is already complete from Phase 15-03.

Purpose: Close the traceability gap where these 14 requirement IDs appear in the traceability table but lack definition bullets in the v1 section. Confirm SELL_PARTIAL dashboard integration needs no further work.
Output: Updated REQUIREMENTS.md with all 14 definitions; verification confirmation of SELL_PARTIAL wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-sell-partial-traceability/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill DRY and UI requirement definitions into REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state -- definitions missing, traceability table present)
    - .planning/phases/12-dry-run-functionality/12-CONTEXT.md (source material for DRY definitions)
    - .planning/phases/13-ui-rework/13-CONTEXT.md (source material for UI definitions)
    - .planning/ROADMAP.md (Phase 12 and 13 goals and success criteria)
  </read_first>
  <action>
Add two new subsections to the v1 Requirements section of `.planning/REQUIREMENTS.md`. Insert them AFTER the existing `### Dashboard` subsection (after line 75, before the `## v2 Requirements` heading).

**Add `### Dry Run` subsection with these 8 definitions (all marked `[x]`):**

```
### Dry Run

- [x] **DRY-01**: Bot supports a `dryRun` config flag that runs the full mainnet pipeline (detection, safety, transaction building) but intercepts before signing/broadcasting
- [x] **DRY-02**: Dry-run buy interception occurs at `broadcastAndConfirm()` (Gate 1), returning a synthetic BroadcastResult with placeholder signature
- [x] **DRY-03**: Dry-run sell interception occurs at `jitoSell()` (Gate 2), preventing Jito bundle submission
- [x] **DRY-04**: Dry-run trades persist to SQLite with `dry_run=1` column and follow the full state machine (BUYING -> MONITORING -> SELLING -> COMPLETED)
- [x] **DRY-05**: Dry-run trades enter PositionManager for shadow price tracking via Jupiter quote polling, showing what P&L would have been
- [x] **DRY-06**: Crash recovery skips dry-run trades on restart (shadow tracking is ephemeral within a session)
- [x] **DRY-07**: Dashboard displays dry-run trades inline in Live Feed with a DRY RUN badge and shows a prominent mode banner when dry-run is enabled
- [x] **DRY-08**: Dashboard header stats (total P&L, win rate, trade count) exclude dry-run trades; `dryRun` is toggleable from Settings
```

**Add `### UI` subsection immediately after Dry Run with these 6 definitions (all marked `[x]`):**

```
### UI

- [x] **UI-01**: Dashboard uses sidebar + content layout (replacing horizontal tabs) with fixed sidebar navigation for Feed, Performance, and Settings views
- [x] **UI-02**: Live Feed displays rich expandable cards with token mint (clickable Solscan/pump.fun links), safety score, source badge, buy amount, P&L, event type badge, and timestamp
- [x] **UI-03**: Performance view includes P&L chart over time (lightweight-charts), win rate visualization, and sortable completed trade history table
- [x] **UI-04**: Trade history table shows per-trade breakdown: entry price, exit price, duration held, P&L in SOL
- [x] **UI-05**: Dashboard uses an industrial/utilitarian trading terminal aesthetic (amber primary, Share Tech Mono + Rajdhani fonts, dark theme)
- [x] **UI-06**: Sidebar collapses responsively at 1024px breakpoint via CSS custom property override
```

**Update the Coverage summary** (near the bottom of the file, currently at line 184-188):
Change `Pending: 2 (DASH-04, DASH-05 -> Phase 15)` to `Pending: 0` since DASH-04 and DASH-05 are already marked complete in the traceability table.

**Update the `*Last updated:` footer** to: `*Last updated: 2026-03-23 after Phase 16 (DRY-01-08, UI-01-06 definitions backfilled)*`

Per D-05: Definitions derived from what was actually built in Phases 12 and 13.
Per D-07: All marked `[x]` since phases are verified complete.
Per D-08: Traceability table entries already exist -- only definition bullets added.
Per D-06: Sections placed after Dashboard (Claude's discretion on positioning).
  </action>
  <verify>
    <automated>grep -c "DRY-0[1-8]" .planning/REQUIREMENTS.md | grep -q "16" && grep -c "UI-0[1-6]" .planning/REQUIREMENTS.md | grep -q "12" && grep "Pending: 0" .planning/REQUIREMENTS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `### Dry Run` section header between `### Dashboard` and `## v2 Requirements`
    - REQUIREMENTS.md contains `### UI` section header after `### Dry Run`
    - REQUIREMENTS.md contains exactly 8 lines matching `DRY-0[1-8]` in the definitions section (8 in definitions + 8 in traceability table = 16 total)
    - REQUIREMENTS.md contains exactly 6 lines matching `UI-0[1-6]` in the definitions section (6 in definitions + 6 in traceability table = 12 total)
    - All 14 new definition lines start with `- [x] **`
    - Coverage summary line reads `Pending: 0`
    - Last updated footer contains `2026-03-23`
  </acceptance_criteria>
  <done>All 14 DRY and UI requirement definitions present in REQUIREMENTS.md v1 section, all marked complete, coverage summary accurate</done>
</task>

<task type="auto">
  <name>Task 2: Verify SELL_PARTIAL frontend wiring is complete</name>
  <files></files>
  <read_first>
    - dashboard/src/store/feed.ts (SSE subscription -- SELL_PARTIAL in eventTypes array)
    - dashboard/src/components/FeedCard.tsx (BADGE_COLORS and EVENT_LABELS for SELL_PARTIAL)
    - src/execution/sell/sell-ladder.ts (backend SELL_PARTIAL emission)
  </read_first>
  <action>
Verify (read-only, no code changes) that all three integration points for SELL_PARTIAL are in place. Per D-01 and D-04, this is already complete from Phase 15-03. Specifically confirm:

1. `dashboard/src/store/feed.ts` line ~36: `eventTypes` array includes the string `'SELL_PARTIAL'`
2. `dashboard/src/components/FeedCard.tsx` BADGE_COLORS object: `SELL_PARTIAL: 'var(--green)'`
3. `dashboard/src/components/FeedCard.tsx` EVENT_LABELS object: `SELL_PARTIAL: 'PARTIAL'`
4. `src/execution/sell/sell-ladder.ts`: contains `botEventBus.emit('event'` with `type: 'SELL_PARTIAL'`

Per D-02: FeedCard renders SELL_PARTIAL via generic fields (detail, pnlSol, isDryRun) -- no SELL_PARTIAL-specific rendering logic needed.
Per D-03: pnlSol shows SOL received for that tier; detail string shows running total. No backend change.

This task produces no file changes. It confirms the existing wiring is complete and documents the verification in the plan summary.
  </action>
  <verify>
    <automated>grep -q "'SELL_PARTIAL'" dashboard/src/store/feed.ts && grep -q "SELL_PARTIAL" dashboard/src/components/FeedCard.tsx && grep -q "SELL_PARTIAL" src/execution/sell/sell-ladder.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/store/feed.ts` contains `'SELL_PARTIAL'` in the eventTypes array
    - `dashboard/src/components/FeedCard.tsx` contains `SELL_PARTIAL:` in both BADGE_COLORS and EVENT_LABELS objects
    - `src/execution/sell/sell-ladder.ts` contains `type: 'SELL_PARTIAL'` in a botEventBus.emit call
    - No files modified by this task (verification only)
  </acceptance_criteria>
  <done>SELL_PARTIAL frontend wiring confirmed complete across SSE subscription, badge color, event label, and backend emission -- no further work needed</done>
</task>

</tasks>

<verification>
1. `grep -c "### Dry Run" .planning/REQUIREMENTS.md` returns 1
2. `grep -c "### UI" .planning/REQUIREMENTS.md` returns at least 1 (in v1 section)
3. All 14 requirement IDs (DRY-01 through DRY-08, UI-01 through UI-06) appear as definition bullets with `[x]` checkmarks
4. Coverage summary shows `Pending: 0`
5. SELL_PARTIAL appears in feed.ts eventTypes, FeedCard.tsx BADGE_COLORS + EVENT_LABELS, and sell-ladder.ts emission
</verification>

<success_criteria>
- REQUIREMENTS.md contains complete definitions for all 14 DRY/UI requirements, all marked complete
- Coverage summary accurately reflects zero pending requirements
- SELL_PARTIAL frontend integration verified as already complete (no code changes needed)
- Phase 16 gap closure is complete
</success_criteria>

<output>
After completion, create `.planning/phases/16-sell-partial-traceability/16-01-SUMMARY.md`
</output>
