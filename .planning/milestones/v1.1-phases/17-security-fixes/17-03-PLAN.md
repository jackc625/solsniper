---
phase: 17-security-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - package.json
  - BUGS.md
autonomous: true
requirements:
  - SEC-04

must_haves:
  truths:
    - "`pnpm audit --audit-level high` reports zero high/critical vulnerabilities"
    - "All moderate vulnerabilities either resolved or documented with justification in BUGS.md"
    - "bigint-buffer HIGH vulnerability documented as accepted risk with LOW real-world impact rationale"
    - "BUGS.md reflects resolution status for all 4 original security findings"
  artifacts:
    - path: "package.json"
      provides: "pnpm overrides for picomatch and brace-expansion, upgraded fastify"
      contains: "overrides"
    - path: "BUGS.md"
      provides: "Updated security audit with per-finding resolution status"
      contains: "RESOLVED"
  key_links:
    - from: "package.json pnpm.overrides"
      to: "pnpm-lock.yaml"
      via: "pnpm install regenerates lockfile with overridden versions"
      pattern: "picomatch"
---

<objective>
Resolve all dependency vulnerabilities that have available fixes, document the unfixable bigint-buffer vulnerability, and update BUGS.md with resolution status for all 4 original security findings (SEC-04 + D-13).

Purpose: Achieve zero high/critical dependency vulnerabilities and provide a clean audit trail. Also consolidate documentation for SEC-01, SEC-02, SEC-03 resolutions from Plans 01 and 02.
Output: Clean `pnpm audit`, updated BUGS.md with all findings resolved/documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-security-fixes/17-CONTEXT.md
@.planning/phases/17-security-fixes/17-RESEARCH.md
@.planning/phases/17-security-fixes/17-01-SUMMARY.md
@.planning/phases/17-security-fixes/17-02-SUMMARY.md

<interfaces>
<!-- Key context the executor needs. -->

From package.json (current state -- Plan 01 will have added eslint devDeps):
```json
{
  "dependencies": {
    "fastify": "^5.8.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3"]
  }
}
```

From 17-RESEARCH.md dependency audit:
| Package | Severity | Fix Available? | Strategy |
|---------|----------|----------------|----------|
| bigint-buffer | HIGH | NO (patched: <0.0.0) | Document-and-accept |
| picomatch 2.3.1 | HIGH | YES (>=2.3.2) | pnpm override |
| picomatch 4.0.3 | HIGH | YES (>=4.0.4) | pnpm override |
| fastify 5.8.2 | MODERATE | YES (>=5.8.3) | Upgrade to 5.8.4 |
| brace-expansion 5.0.4 | MODERATE | YES (>=5.0.5) | pnpm override |

pnpm overrides format:
```json
{
  "pnpm": {
    "overrides": {
      "picomatch@<2.3.2": "2.3.2",
      "picomatch@>=4.0.0 <4.0.4": "4.0.4",
      "brace-expansion@>=4.0.0 <5.0.5": "5.0.5"
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dependency vulnerabilities via upgrades and overrides</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file -- current dependencies, devDependencies, pnpm config section)
  </read_first>
  <action>
1. Upgrade fastify to 5.8.4 (fixes moderate X-Forwarded-Proto/Host spoofing, per D-11):
   ```bash
   pnpm update fastify
   ```

2. Add pnpm overrides to package.json for transitive dependency vulnerabilities. Merge into the existing `"pnpm"` section (which already has `"onlyBuiltDependencies"`):
   ```json
   "pnpm": {
     "onlyBuiltDependencies": ["better-sqlite3"],
     "overrides": {
       "picomatch@<2.3.2": "2.3.2",
       "picomatch@>=4.0.0 <4.0.4": "4.0.4",
       "brace-expansion@>=4.0.0 <5.0.5": "5.0.5"
     }
   }
   ```

3. Run `pnpm install` to regenerate the lockfile with overridden versions. This MUST happen after adding overrides (per research Pitfall #4).

4. Run `pnpm audit` to verify results:
   - Expected: bigint-buffer HIGH remains (no fix available)
   - Expected: picomatch HIGH/MODERATE resolved
   - Expected: fastify MODERATE resolved
   - Expected: brace-expansion MODERATE resolved

5. Run `pnpm audit --audit-level high` to check specifically for high/critical:
   - Expected: only bigint-buffer HIGH remains (unfixable)

6. Run the full test suite to ensure nothing broke:
   ```bash
   pnpm test
   ```

7. Run the typecheck to ensure no type regressions:
   ```bash
   pnpm exec tsc --noEmit
   ```

Note on bigint-buffer (per D-12, research finding): The vulnerability is a buffer overflow in `toBigIntLE()`. In this codebase it's used by `@solana/buffer-layout-utils` for deserializing on-chain data from RPC responses -- NOT user input. spl-token 0.4.14 is the latest version (no 0.5.x exists), and the patched version field shows `<0.0.0` (no fix released). Accept this vulnerability with documentation.
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && rtk pnpm test && rtk pnpm audit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "overrides" key inside "pnpm" section
    - package.json pnpm.overrides contains "picomatch@<2.3.2": "2.3.2"
    - package.json pnpm.overrides contains "picomatch@>=4.0.0 <4.0.4": "4.0.4"
    - package.json pnpm.overrides contains "brace-expansion@>=4.0.0 <5.0.5": "5.0.5"
    - package.json fastify dependency resolves to >= 5.8.3 (check pnpm list fastify)
    - `pnpm audit` shows picomatch and brace-expansion vulnerabilities resolved
    - `pnpm audit` shows fastify vulnerability resolved
    - `pnpm audit` only remaining finding is bigint-buffer (unfixable)
    - `pnpm test` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>All fixable dependency vulnerabilities resolved. Fastify upgraded to 5.8.4, picomatch overridden to 2.3.2/4.0.4, brace-expansion overridden to 5.0.5. Only bigint-buffer HIGH remains (unfixable, accepted risk).</done>
</task>

<task type="auto">
  <name>Task 2: Update BUGS.md with resolution status for all 4 security findings</name>
  <files>BUGS.md</files>
  <read_first>
    - BUGS.md (current content -- the original ship-safe audit with 4 real findings and false positives)
    - .planning/phases/17-security-fixes/17-01-SUMMARY.md (SEC-01 + SEC-02 resolution details)
    - .planning/phases/17-security-fixes/17-02-SUMMARY.md (SEC-03 resolution details)
    - .planning/phases/17-security-fixes/17-RESEARCH.md (SQL audit table, fetch audit table, dependency audit table)
  </read_first>
  <action>
Rewrite BUGS.md to reflect completed Phase 17 security audit (per D-02, D-13). The file should:

1. Keep the original audit context (Ship-Safe Audit) but add a Phase 17 resolution section.

2. For each of the 4 real findings, add a resolution status:

   **Finding 1: SQL injection via template literal (trade-store.ts:100)**
   - Status: **RESOLVED -- False Positive Confirmed**
   - Resolution: Full codebase SQL audit confirmed all 14 prepared statements in trade-store.ts use `@named` parameters. The flagged line 100 generates `?` placeholders from `NON_TERMINAL_STATES.map(() => '?')` -- a hardcoded array with no user input. Additional SQL in schema.ts (static DDL strings) and trades.ts (static prepared statements) also confirmed safe. ESLint custom rule `security/no-sql-template-literals` added as regression guard.
   - Files audited: `src/persistence/trade-store.ts`, `src/persistence/schema.ts`, `src/dashboard/routes/trades.ts`
   - Phase: 17, Plan 01

   **Finding 2: API key in URL query param (tier3-creator.ts:112)**
   - Status: **RESOLVED -- Fixed**
   - Resolution: Helius API key migrated from `?api-key=` query parameter to `X-Api-Key` request header. URL no longer contains credentials. `safeUrl` masking code removed (no longer needed). ESLint custom rule `security/no-api-key-in-url` added as regression guard. Full fetch() audit confirmed all other API calls (Jupiter, RugCheck, PumpPortal, Jito, RPC) are already safe.
   - Files modified: `src/safety/checks/tier3-creator.ts`
   - Phase: 17, Plan 01

   **Finding 3: No request body validation (config.ts:42)**
   - Status: **RESOLVED -- Fixed**
   - Resolution: Config PATCH endpoint now has 3-layer validation: (1) patch body shape via ConfigPatchSchema, (2) merged result validated against full TradingConfigSchema, (3) cross-field semantic checks (safety weights sum = 100, tiered TP pct sum <= 100). Invalid patches trigger rollback to previous config via `restoreRuntimeConfig()`. Zod validation errors formatted as human-friendly strings.
   - Files modified: `src/dashboard/routes/config.ts`, `src/config/trading.ts`
   - Phase: 17, Plan 02

   **Finding 4: Dependency vulnerabilities (package.json)**
   - Status: **RESOLVED -- Fixed (1 accepted)**
   - Resolution: Fastify upgraded from 5.8.2 to 5.8.4 (fixes moderate X-Forwarded-Proto/Host spoofing). Picomatch HIGH/MODERATE resolved via pnpm overrides (2.3.2, 4.0.4). Brace-expansion MODERATE resolved via pnpm override (5.0.5). One accepted risk: bigint-buffer HIGH (buffer overflow in `toBigIntLE()`) -- no patched version exists (`patched: <0.0.0`), spl-token 0.4.14 is the latest version with no upgrade path. Real-world risk is LOW because bigint-buffer is used for deserializing on-chain data from RPC responses (not user input).
   - Phase: 17, Plan 03

3. Keep the "Almost Certainly False Positives" section unchanged -- it documents what was analyzed and dismissed.

4. Add a summary line at the top:
   ```
   Security Audit Status: ALL FINDINGS RESOLVED (Phase 17, 2026-03-27)
   ```

5. Run the final full verification:
   ```bash
   pnpm test && pnpm exec eslint src/ && pnpm audit
   ```
  </action>
  <verify>
    <automated>cd C:/Users/jackc/Code/solsniper && rtk pnpm test && rtk pnpm exec eslint src/ 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - BUGS.md contains "RESOLVED" for all 4 findings
    - BUGS.md contains "False Positive Confirmed" for SQL injection finding
    - BUGS.md contains "X-Api-Key" referencing the API key migration
    - BUGS.md contains "3-layer validation" or "merged result" referencing config fix
    - BUGS.md contains "bigint-buffer" with accepted risk justification mentioning "no patched version"
    - BUGS.md contains "Security Audit Status: ALL FINDINGS RESOLVED"
    - `pnpm test` exits 0
    - `pnpm exec eslint src/` exits 0
  </acceptance_criteria>
  <done>BUGS.md updated with resolution status for all 4 security findings. SQL injection confirmed as false positive, API key migrated to header, config validation hardened, dependency vulns resolved (1 accepted risk documented). Full test suite and ESLint pass.</done>
</task>

</tasks>

<verification>
1. `pnpm audit --audit-level high` -- only bigint-buffer remains (accepted)
2. `pnpm audit` -- picomatch, fastify, brace-expansion all resolved
3. `pnpm test` -- full test suite passes
4. `pnpm exec eslint src/` -- zero violations
5. BUGS.md has RESOLVED status for all 4 findings
6. `grep -c "RESOLVED" BUGS.md` -- returns 4 (one per finding)
</verification>

<success_criteria>
- Zero fixable high/critical dependency vulnerabilities
- Fastify upgraded, picomatch and brace-expansion overridden
- bigint-buffer documented as accepted risk with rationale
- BUGS.md reflects all 4 security findings as resolved/documented
- Full test suite green, ESLint clean, audit results match expectations
</success_criteria>

<output>
After completion, create `.planning/phases/17-security-fixes/17-03-SUMMARY.md`
</output>
