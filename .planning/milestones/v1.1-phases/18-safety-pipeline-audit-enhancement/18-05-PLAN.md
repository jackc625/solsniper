---
phase: 18-safety-pipeline-audit-enhancement
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/safety-audit-2026-03-30.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements:
  - SAF-10

must_haves:
  truths:
    - "An audit report exists in reports/ documenting safety pass/fail rates against actual trade outcomes"
    - "SAF-10 is marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "reports/safety-audit-2026-03-30.md"
      provides: "Safety pipeline audit report with accuracy analysis"
      contains: "# Safety Pipeline Audit Report"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement tracking"
      contains: "[x] **SAF-10**"
  key_links:
    - from: "scripts/audit-safety.ts"
      to: "data/trades.db"
      via: "better-sqlite3 readonly query"
      pattern: "readonly.*true"
    - from: "scripts/audit-safety.ts"
      to: "reports/safety-audit-2026-03-30.md"
      via: "writeReport fs.writeFileSync"
      pattern: "safety-audit-"
---

<objective>
Close the single remaining gap in Phase 18: run the existing audit script against the production database to generate the first safety audit report, completing SAF-10.

Purpose: The audit tooling (scripts/audit-safety.ts) is fully implemented and tested (15/15 tests pass), but the actual report has never been generated. ROADMAP.md Success Criterion 1 requires "An audit report exists documenting current safety pass/fail rates" -- not just the tooling.

Output: reports/safety-audit-2026-03-30.md (generated audit report), updated REQUIREMENTS.md marking SAF-10 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-safety-pipeline-audit-enhancement/18-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run audit script to generate the safety audit report</name>
  <files>reports/safety-audit-2026-03-30.md</files>
  <read_first>
    - scripts/audit-safety.ts (understand CLI interface and output format)
    - data/trades.db (confirm file exists -- do NOT modify, readonly access only)
  </read_first>
  <action>
Run the existing audit script against the production SQLite database to generate the first safety audit report.

Command to execute:
```
npx tsx scripts/audit-safety.ts --db data/trades.db --output reports/
```

The script:
1. Reads from data/trades.db in readonly mode (better-sqlite3 with `{ readonly: true }`)
2. Queries COMPLETED/FAILED/ABANDONED trades for P&L correlation
3. Since no --logs flag is provided (no pino log file available -- see RESEARCH.md Open Question 2), uses DB safety columns only (safety_score, safety_rejection_reasons, safety_checks_detail added by Plan 01)
4. Creates reports/ directory via `fs.mkdirSync(outputDir, { recursive: true })`
5. Writes Markdown report to `reports/safety-audit-YYYY-MM-DD.md`

If the script fails because trades table has no completed trades with safety data (the safety columns were added in this phase, so existing completed trades may have NULL values), that is an expected outcome -- the report should still generate but note the limited data. The script handles this gracefully (computeStats works with empty/sparse data).

After report generation, read the report to confirm it was written and contains the expected sections: Summary, Trade Outcomes, Per-Check Accuracy, Score Distribution, Recommendations.

Do NOT modify the audit script or the trades database. This is purely an execution task.
  </action>
  <verify>
    <automated>ls reports/safety-audit-*.md && head -30 reports/safety-audit-*.md</automated>
  </verify>
  <acceptance_criteria>
    - File `reports/safety-audit-2026-03-30.md` exists
    - File contains `# Safety Pipeline Audit Report`
    - File contains `## Summary` section
    - File contains `## Recommendations` section
    - File size is greater than 500 bytes (not an empty/stub report)
  </acceptance_criteria>
  <done>A safety audit report exists in reports/ with summary stats documenting trade outcomes and safety pipeline accuracy</done>
</task>

<task type="auto">
  <name>Task 2: Mark SAF-10 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of requirements)
  </read_first>
  <action>
Update .planning/REQUIREMENTS.md to mark SAF-10 as complete:

1. Change line 19 from:
   `- [ ] **SAF-10**: Current safety pipeline pass/fail rates audited against real trade outcomes to identify false positives and false negatives`
   to:
   `- [x] **SAF-10**: Current safety pipeline pass/fail rates audited against real trade outcomes to identify false positives and false negatives`

2. Change line 89 in the Traceability table from:
   `| SAF-10 | Phase 18 | Pending |`
   to:
   `| SAF-10 | Phase 18 | Complete |`
  </action>
  <verify>
    <automated>grep "SAF-10" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `[x] **SAF-10**` (not `[ ] **SAF-10**`)
    - .planning/REQUIREMENTS.md Traceability table row for SAF-10 reads `Complete` (not `Pending`)
  </acceptance_criteria>
  <done>SAF-10 is marked as complete in REQUIREMENTS.md with both the checkbox and traceability table updated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review of generated audit report</name>
  <files>reports/safety-audit-2026-03-30.md</files>
  <action>
Present the generated audit report to the user for review. The safety audit report was generated by running scripts/audit-safety.ts against the production trades database (data/trades.db). The report documents safety pipeline accuracy based on real trade outcome data.

What was built: The audit script was executed (not modified) to produce the first safety audit report from production data. This closes the SAF-10 gap -- tooling existed but had never been run.

How to verify:
1. Open `reports/safety-audit-2026-03-30.md` and review the contents
2. Confirm the report contains:
   - Summary section with pass/fail rates (may show limited data if most trades predate Phase 18 safety columns)
   - Trade Outcomes with P&L breakdown
   - Per-Check Accuracy table
   - Score Distribution
   - Recommendations section
3. If the report notes limited data (e.g., "0 trades with safety data"), that is expected -- the safety columns were added during Phase 18, so pre-existing completed trades have NULL safety values. The report is still valid as it documents this baseline.
4. Verify the analysis is coherent and the recommendations section is present

Resume signal: Type "approved" if the report is acceptable, or describe any issues.
  </action>
  <verify>
    <automated>ls reports/safety-audit-*.md</automated>
  </verify>
  <done>User has reviewed and approved the generated audit report content</done>
</task>

</tasks>

<verification>
- `ls reports/safety-audit-*.md` shows exactly one report file
- `grep "# Safety Pipeline Audit Report" reports/safety-audit-*.md` succeeds
- `grep "\[x\] \*\*SAF-10\*\*" .planning/REQUIREMENTS.md` succeeds
- `grep "SAF-10.*Complete" .planning/REQUIREMENTS.md` succeeds
</verification>

<success_criteria>
- An audit report exists at reports/safety-audit-2026-03-30.md with Summary, Trade Outcomes, Per-Check Accuracy, Score Distribution, and Recommendations sections
- SAF-10 is marked [x] in REQUIREMENTS.md
- Human has reviewed and approved the report content
</success_criteria>

<output>
After completion, create `.planning/phases/18-safety-pipeline-audit-enhancement/18-05-SUMMARY.md`
</output>
